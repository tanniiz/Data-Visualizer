<UserControl x:Class="DataVisualizer.UI.ApplicationShell.ApplicationShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataVisualizer.UI.ApplicationShell"
             xmlns:control="clr-namespace:DataVisualizer.UI.Controls"
             mc:Ignorable="d" 
             Width="640"
             Height="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <control:FileBrowserButton FileFilter="{Binding filter}" DefaultFileFilter="{Binding filter}"></control:FileBrowserButton>          
    </Grid>
</UserControl>
